var GithubSearch = require('GithubSearch'),
	github = new GithubSearch();

var options = {
	q: 'tetris', //TODO: query as object not string?
	sort: 'stars',
	order: 'desc'
};

github.searchRepos(options, function(data) {
	console.log(data);
});